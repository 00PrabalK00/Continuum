# Continuum v0.6.2

Release date: 2026-05-28

## Fixed

- Normalized the PTY input receipt acceptance test's working-directory
  comparison so macOS `/var` and `/private/var` temp path aliases do not fail
  an otherwise valid same-session receipt.

## Verification

- `python -m unittest tests.test_terminal -v`
- `python -m unittest discover -s tests -v`
- GitHub Actions matrix for Windows, Ubuntu and macOS.
